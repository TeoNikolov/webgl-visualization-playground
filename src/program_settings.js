var mozaicProgramSettings = {
	gridX: {type:'number', value: 32, valueMin: 1},
	gridY: {type:'number', value: 18, valueMin: 1}
};

var ringProgramSettings = {
};

var programSettings = {
	mozaicProgram: mozaicProgramSettings,
	ringProgram: ringProgramSettings
}